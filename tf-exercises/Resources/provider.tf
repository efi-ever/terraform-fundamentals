# This block declares the Azure Resource Manager provider. The features block is used to define settings for the provider and is required. As there are no features currently that can be toggled, it is left empty.
provider "azurerm" {
  features {
  }
}